apiVersion: 1
providers:
  - name: langgraph
    orgId: 1
    folder: LangGraph
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
